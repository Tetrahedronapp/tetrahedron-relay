name = "tetrahedron-relay"
main = "src/index.ts"
compatibility_date = "2024-09-01"

# After deploy, add these as secrets via the Cloudflare dashboard
# (Workers → your-worker → Settings → Variables → "Add variable" → enable encrypt):
#
#   AUTH_TOKEN         required — random hex string, shared with Tetrahedron
#   ANTHROPIC_API_KEY  optional — sk-ant-… (only if you use Anthropic seats)
#   OPENAI_API_KEY     optional — sk-…     (only if you use OpenAI seats)
